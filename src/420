Insert Symbol:
    NameTypeSym  (insertSym (NameTypeSym) -> insertNameTypeSym):
        scope is always 0
        ERROR: name already defined:  nametype

    FuncSym (insertSym (FuncSym) -> insertFuncSym):
      scope is always 1, no body
      ERROR: already defined: a variable or function with same type of arguments

    VarSym in current scope(insertSym (VarSym) -> insertVarSym):
      this happens on arguments declarations and in normal declarations
      ERROR: already defined: a variable in same scope

Modify Symbol:
    complete function (completeFunctionDef):
        save its AST

Look up symbol:
    in given scope(findSymInScope):
      this happens on record access
      ERROR: not defined in that scope
    in closest scope (findSym  -> findVarSym and findNameTypeSym):
      this happens in nametypes use and variable ids (split in two functions)
      ERROR: not defined in reachable scopes

    look up function(findFunction):
      ERROR:  function found but not the correct arguments
              defined as variable or type
              not defined

beginScope
endScope

setReturnType
getReturnType
